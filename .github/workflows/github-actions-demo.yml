name: CI/CD

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    # Точно та же Ubuntu 22.04, что и на проде
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          # Точно та же версия, что на сервере
          node-version: '22.15.0'

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3 libsqlite3-dev

      - name: Install dependencies
        run: npm ci

      - name: Rebuild better-sqlite3 from source (glibc 2.35)
        run: npm rebuild better-sqlite3 --build-from-source --verbose

      - name: Build bundle with ncc
        run: npx ncc build src/index.ts -m -o dist

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
